{
  "name": "chmod-644-octal-is-not-gated",
  "note": "GUARD: octal 644 (owner rw, group/other r) is not world-writable — its last digit (4) does not grant write — so bash-chmod-dangerous does not fire. Fails closed to gommage; it does NOT become an ask_picto for fs.chmod:dangerous.",
  "call": { "tool": "Bash", "input": { "command": "chmod 644 config.txt" } },
  "expected": { "kind": "gommage" }
}
