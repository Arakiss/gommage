{
  "name": "gh-pr-merge-dynamic-repository-fails-closed",
  "call": { "tool": "Bash", "input": { "command": "gh pr merge 79 --repo \"$REPO\" --squash" } },
  "expected": {
    "kind": "gommage",
    "hard_stop": true,
    "matched_rule": "deny-ambiguous-shell-effects"
  }
}
