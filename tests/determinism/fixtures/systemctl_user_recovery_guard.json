{
  "name": "generic-user-service-reload-fails-closed",
  "note": "A generic user service-manager reload is a mutation path, not a recovery inspection command. Without a typed Gommage service target it must not receive a blanket recovery allow.",
  "call": { "tool": "Bash", "input": { "command": "systemctl --user daemon-reload" } },
  "expected": { "kind": "gommage", "hard_stop": false }
}
