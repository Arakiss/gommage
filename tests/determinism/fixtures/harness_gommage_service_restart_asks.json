{
  "name": "harness-gommage-service-restart-asks",
  "note": "Restarting the Gommage daemon changes the live decision authority and must use the semantic reconfiguration gate.",
  "call": { "tool": "Bash", "input": { "command": "systemctl --user restart gommage-daemon.service" } },
  "expected": { "kind": "ask_picto", "required_scope": "gommage.reconfigure", "bind_input": true, "matched_rule": "gate-gommage-reconfigure" }
}
