{
  "name": "shell-compound-scp-remote-requires-every-execution",
  "note": "The remote copy remains visible, but the separate `cd` execution must also be resolved instead of inheriting the egress approval.",
  "call": { "tool": "Bash", "input": { "command": "cd /x && scp f user@host:/p" } },
  "expected": { "kind": "gommage", "hard_stop": false }
}
