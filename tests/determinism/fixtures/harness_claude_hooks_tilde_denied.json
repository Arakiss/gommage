{
  "name": "harness-claude-hooks-tilde-denied",
  "note": "A literal-tilde Bash redirect under ~/.claude/hooks must hit the harness integrity rule, not fall through to a broader allow.",
  "call": { "tool": "Bash", "input": { "command": "printf '#!/bin/sh\n' > ~/.claude/hooks/pretool.sh" } },
  "expected": { "kind": "gommage", "hard_stop": false, "matched_rule": "deny-agent-hook-config-tamper" }
}
