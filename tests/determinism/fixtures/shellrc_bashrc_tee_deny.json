{
  "name": "tee-to-bashrc-is-denied",
  "note": "tee emits a literal-tilde fs.write target; policy evaluation normalizes it to HOME before deny-shell-rc-write matches. Closes the persistence channel where an agent writes its own startup hook.",
  "call": { "tool": "Bash", "input": { "command": "echo payload | tee ~/.bashrc" } },
  "expected": { "kind": "gommage" }
}
