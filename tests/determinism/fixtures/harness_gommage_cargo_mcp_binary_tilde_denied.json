{
  "name": "harness-gommage-cargo-mcp-binary-tilde-denied",
  "note": "The default cargo install path for gommage-mcp is part of the harness decision path.",
  "call": { "tool": "Bash", "input": { "command": "cat > ~/.cargo/bin/gommage-mcp << 'EOF'\n#!/bin/sh\nEOF" } },
  "expected": { "kind": "gommage", "hard_stop": false, "matched_rule": "deny-gommage-binary-tamper" }
}
