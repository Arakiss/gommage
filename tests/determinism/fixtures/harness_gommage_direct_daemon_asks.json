{
  "name": "direct-gommage-daemon-start-requires-input-bound-reconfigure-picto",
  "call": { "tool": "Bash", "input": { "command": "gommage-daemon --foreground --home /tmp/gommage-direct --socket /tmp/gommage-direct.sock" } },
  "expected": {
    "kind": "ask_picto",
    "required_scope": "gommage.reconfigure",
    "bind_input": true,
    "matched_rule": "gate-gommage-reconfigure"
  }
}
