{
  "name": "harness-gommage-launchctl-bootout-asks",
  "note": "Booting out the Gommage LaunchAgent disables the live daemon and must use the semantic disable gate.",
  "call": { "tool": "Bash", "input": { "command": "launchctl bootout gui/501/dev.gommage.daemon" } },
  "expected": { "kind": "ask_picto", "required_scope": "gommage.disable", "bind_input": true, "matched_rule": "gate-gommage-disable" }
}
