{
  "name": "shell-compound-ld-preload-is-denied",
  "note": "Proves shell-awareness for the injection rule: `echo hi && LD_PRELOAD=/x.so ls` still denies. The whole-command candidate carries the LD_PRELOAD prefix even though the segment candidate `ls` has it stripped. Denied via deny-env-inject-preload (hard_stop:false).",
  "call": { "tool": "Bash", "input": { "command": "echo hi && LD_PRELOAD=/x.so ls" } },
  "expected": { "kind": "gommage", "hard_stop": false }
}
