{
  "name": "ordinary-env-prefix-is-not-injection",
  "note": "GUARD: `FOO=bar mycmd` is not preload injection, but an arbitrary environment mutation still changes execution context and therefore fails closed as ambiguity.",
  "call": { "tool": "Bash", "input": { "command": "FOO=bar mycmd" } },
  "expected": { "kind": "gommage", "hard_stop": true, "matched_rule": "deny-ambiguous-shell-effects" }
}
