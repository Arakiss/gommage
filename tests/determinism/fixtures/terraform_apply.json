{
  "name": "terraform-apply-requires-picto",
  "call": { "tool": "Bash", "input": { "command": "terraform apply -auto-approve" } },
  "expected": { "kind": "ask_picto", "required_scope": "terraform.apply" }
}
