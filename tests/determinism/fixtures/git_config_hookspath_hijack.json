{
  "name": "git-config-core-hookspath-asks-for-picto",
  "note": "Setting core.hooksPath points git at an arbitrary hooks directory whose scripts run on routine git operations. Gated via bash-git-config-hijack.",
  "call": { "tool": "Bash", "input": { "command": "git config core.hooksPath /tmp/evil-hooks" } },
  "expected": { "kind": "ask_picto", "required_scope": "git.config:hijack" }
}
