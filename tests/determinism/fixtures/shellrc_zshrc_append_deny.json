{
  "name": "append-to-zshrc-is-denied",
  "note": "R2 emits a literal-tilde fs.write target from the redirect; policy evaluation normalizes it to HOME before deny-shell-rc-write matches. A login/shell rc runs code on every new shell.",
  "call": { "tool": "Bash", "input": { "command": "echo export PROMPT_HOOK=1 >> ~/.zshrc" } },
  "expected": { "kind": "gommage" }
}
