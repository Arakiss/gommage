{
  "name": "gh-pr-merge-admin-requires-picto",
  "call": { "tool": "Bash", "input": { "command": "gh pr merge 79 --repo Arakiss/galdr --squash --admin --delete-branch" } },
  "expected": {
    "kind": "ask_picto",
    "required_scope": "gh.pr.merge.admin",
    "bind_input": true,
    "matched_rule": "gate-gh-pr-merge-admin"
  }
}
