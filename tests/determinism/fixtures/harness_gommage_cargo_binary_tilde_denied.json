{
  "name": "harness-gommage-cargo-binary-tilde-denied",
  "note": "The default cargo install path for the primary gommage binary is part of the harness decision path.",
  "call": { "tool": "Bash", "input": { "command": "cat > ~/.cargo/bin/gommage << 'EOF'\n#!/bin/sh\nEOF" } },
  "expected": { "kind": "gommage", "hard_stop": false, "matched_rule": "deny-gommage-binary-tamper" }
}
