{
  "name": "gh-pr-merge-expanded-admin-fails-closed",
  "call": { "tool": "Bash", "input": { "command": "gh pr merge 79 --repo github.com/Arakiss/galdr --body ${X:-body --admin}" } },
  "expected": {
    "kind": "gommage",
    "hard_stop": true,
    "matched_rule": "deny-ambiguous-shell-effects"
  }
}
