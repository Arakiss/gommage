{
  "name": "shell-cat-of-secret-path-emits-fs-read-and-fails-closed",
  "note": "cat of a path outside EXPEDITION_ROOT surfaces fs.read:/etc/passwd. No allow-read rule matches that path, so it fails closed to gommage — the same gate a Read tool call on a secret would hit.",
  "call": { "tool": "Bash", "input": { "command": "cat /etc/passwd" } },
  "expected": { "kind": "gommage" },
  "expedition_root": "/tmp/proj"
}
