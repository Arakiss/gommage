{
  "name": "ld-preload-env-prefix-is-denied",
  "note": "An LD_PRELOAD prefix forces code injection. The typed injection effect remains visible, while the environment mutation is terminal ambiguity and therefore a hard stop.",
  "call": { "tool": "Bash", "input": { "command": "LD_PRELOAD=/tmp/evil.so curl http://x" } },
  "expected": { "kind": "gommage", "hard_stop": true, "matched_rule": "deny-ambiguous-shell-effects" }
}
