{
  "name": "shell-compound-crontab-edit-requires-every-execution",
  "note": "The crontab edit remains visible, but the separate `cd` execution must also be resolved instead of inheriting the crontab approval.",
  "call": { "tool": "Bash", "input": { "command": "cd /x && crontab -e" } },
  "expected": { "kind": "gommage", "hard_stop": false }
}
