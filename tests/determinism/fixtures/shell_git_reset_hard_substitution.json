{
  "name": "shell-substitution-git-reset-hard-fails-closed-as-dynamic-command",
  "call": { "tool": "Bash", "input": { "command": "$(git reset --hard origin/main)" } },
  "expected": {
    "kind": "gommage",
    "hard_stop": true,
    "matched_rule": "deny-ambiguous-shell-effects"
  }
}
