{
  "name": "dyld-insert-libraries-env-prefix-is-denied",
  "note": "DYLD_INSERT_LIBRARIES injects a dylib. The typed injection effect remains visible, while the environment mutation is terminal ambiguity and therefore a hard stop.",
  "call": { "tool": "Bash", "input": { "command": "DYLD_INSERT_LIBRARIES=/tmp/e.dylib ls" } },
  "expected": { "kind": "gommage", "hard_stop": true, "matched_rule": "deny-ambiguous-shell-effects" }
}
