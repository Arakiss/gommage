{
  "name": "shell-cat-inside-expedition-root-is-allowed",
  "note": "cat of a file under EXPEDITION_ROOT surfaces both fs.read:<root>/... and the whole-command proc.exec capability. allow-shell-project-reads matches that capability set; allow-project-reads independently permits the normalized fs.read capability. Guards that fs.read extraction does not over-deny legitimate in-project reads.",
  "call": { "tool": "Bash", "input": { "command": "cat /tmp/proj/src/lib.rs" } },
  "expected": { "kind": "allow" },
  "expedition_root": "/tmp/proj"
}
